{
  "name": "pob-codes",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "test": "npm run --workspaces test",
    "typecheck": "npm run --workspaces typecheck",
    "sync:data": "node scripts/sync-data.mjs",
    "update:local-pob-mirror": "node scripts/update-local-pob-mirror.mjs",
    "update:pob-upstream": "node scripts/update-local-pob-mirror.mjs",
    "verify:assets": "node scripts/verify-assets.mjs",
    "verify:unique-art": "node scripts/verify-unique-art-coverage.mjs",
    "dev:restart": "restart-dev.cmd",
    "dev:web": "npm --workspace @pobcodes/web run dev",
    "dev:worker": "npm --workspace @pobcodes/worker run dev"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/pako": "^2.0.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}

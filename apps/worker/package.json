{
  "name": "@pobcodes/worker",
  "version": "0.1.0",
  "license": "GPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --config .wrangler/deploy.toml"
  },
  "dependencies": {
    "@pobcodes/pob-codec": "0.1.0",
    "@pobcodes/pob-parser": "0.1.0",
    "@pobcodes/shared-types": "0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260211.0",
    "wrangler": "^4.79.0"
  }
}
